{
  "name": "phoenix-image-processor",
  "version": "1.0.0",
  "description": "Lambda function for processing images",
  "main": "src/index.js",
  "scripts": {
    "test": "jest --coverage"
  },
  "dependencies": {
    "aws-sdk": "^2.1574.0",
    "sharp": "^0.33.2"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "node",
    "coverageThreshold": {
      "global": {
        "branches": 80,
        "functions": 80,
        "lines": 80,
        "statements": 80
      }
    }
  }
}
