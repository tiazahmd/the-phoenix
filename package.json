{
  "name": "the-phoenix",
  "version": "1.0.0",
  "description": "The Phoenix Project - AWS Lambda Functions",
  "private": true,
  "scripts": {
    "test": "cd lambda/image-processor && npm test && cd ../notification-handler && npm test",
    "lint": "eslint lambda/image-processor lambda/notification-handler"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "eslint-config-airbnb-base": "^15.0.0",
    "eslint-plugin-import": "^2.29.1"
  }
}
