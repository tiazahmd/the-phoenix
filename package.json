{
  "name": "the-phoenix",
  "version": "1.0.0",
  "description": "The Phoenix Project - AWS Lambda Functions",
  "private": true,
  "workspaces": [
    "lambda/*"
  ],
  "scripts": {
    "test": "cd lambda/image-processor && npm test && cd ../notification-handler && npm test",
    "lint": "eslint lambda/*/src/**/*.js"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "eslint-config-airbnb-base": "^15.0.0",
    "eslint-plugin-import": "^2.29.1"
  }
}
