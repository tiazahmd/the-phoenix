{
  "name": "the-phoenix",
  "version": "1.0.0",
  "description": "The Phoenix Project - AWS Lambda Functions",
  "private": true,
  "scripts": {
    "test": "cd lambda/image-processor && npm test && cd ../notification-handler && npm test",
    "lint": "cd lambda/image-processor && npm run lint && cd ../notification-handler && npm run lint"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "eslint-config-airbnb-base": "^15.0.0",
    "eslint-plugin-import": "^2.29.1"
  }
}
